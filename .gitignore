# build artifacts
build/
vendor/raylib/

# Game content (models/sounds/maps) IS versioned so a fresh clone is playable.
# Only the working-copy .glb backups stay local.
*.glb.bak

# per-machine live state
vm_tune.txt
vm_tune_*.txt
vm_tune*.txt.pin
options.txt
chernobyl2-debug.log
chernobyl2-shot.png

# tooling binaries / deps
tools/FBX2glTF
tools/node_modules
spawn*.txt
